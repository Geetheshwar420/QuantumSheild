name: quantumshield_mobile
description: A post-quantum secure chat and file transfer application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Networking
  socket_io_client: ^1.0.2
  http: ^1.1.0
  
  # State Management
  provider: ^6.0.0
  
  # UI & Design
  flutter_svg: ^2.0.5
  cached_network_image: ^3.2.3
  shimmer: ^3.0.0
  
  # Local Storage
  shared_preferences: ^2.1.0
  sqflite: ^2.2.5
  
  # Utilities
  intl: ^0.18.0
  uuid: ^3.0.7
  get: ^4.6.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # assets:
  #   - assets/images/
  #   - assets/icons/

  # fonts:
  #   - family: Poppins
  #     fonts:
  #       - asset: assets/fonts/Poppins-Regular.ttf
  #       - asset: assets/fonts/Poppins-Bold.ttf
  #         weight: 700
